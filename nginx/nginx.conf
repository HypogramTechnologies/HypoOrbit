server {
    listen 80;

    location /api/ {
        proxy_pass http://backend-hypoorbit:3000/api/;
    }

    location / {
        root /var/www/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
